#lang sicp

;; 第一课作业 —— 做完把答案填在下面，发给我一起看

;; 练习 A：把下面用前缀写法写出来（在交互区验证）
;;   1. (3 + 4) * 5  
;;   2. 1 + 2 + 3 + 4
;;   3. (2 + 3) * (4 + 5)


;; 练习 B：定义过程 cube，计算 x 的立方
;; (define (cube x) ...)
;; 验证：(cube 3) 应为 27


;; 练习 C：定义过程 max2，返回两个数中较大的那个
;; (define (max2 a b) ...)
;; 验证：(max2 3 7) 应为 7；(max2 9 2) 应为 9

A:
1. (* (+ 3 4) 5)
2. (+ 1 2 3 4)
3. (* (+ 2 3)(+ 4 5))
B:
(define (cube x)
    (* x x x))
(define (max2 a b)
    (if (> a b))
        a
        b))